export enum USER_ROUTES {
  BASE = 'user',
  CHANGE_PASSWORD = 'password',
}

export enum AUTH_ROUTES {
  BASE = '/auth',
  REGISTER = '/register',
  LOG_IN = '/log-in',
}

export enum SUBJECT_ROUTES {
  BASE = '/subject',
}
